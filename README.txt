To run the application, go to the command line and cd
into the directory for this repo.


Then run the command: pip install -r requirements.txt

Afterwards, run this command: python main.py


After a couple of seconds, the Review Insights app
should open in a new tab on your machine.